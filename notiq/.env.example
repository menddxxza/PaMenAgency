# Notiq — variables de entorno.
# Copia este fichero a .env.local y rellena lo que necesites.

# --- Base de datos (Neon) ----------------------------------------------------
# Sin esta la app arranca, pero enseña un aviso de configuración en lugar de la
# aplicación. Connection string de tu proyecto en https://neon.tech (Dashboard →
# Connect). Aplica el esquema con:
#   psql "$DATABASE_URL" -f migrations/0001_neon.sql
DATABASE_URL=

# --- Autenticación (Auth.js) --------------------------------------------------
# Clave para firmar las sesiones. Genera una con:
#   npx auth secret
# o, sin el CLI de Auth.js:
#   openssl rand -base64 33
AUTH_SECRET=

# --- IA ---------------------------------------------------------------------
# Groq es el proveedor por defecto: rápido, con plan gratuito, y el chat del
# asistente lleva buscador web integrado sin depender de nada más. Solo hace falta
# esta clave — el resto de variables de abajo ya vienen puestas al modelo correcto
# de Groq para cada caso, no hace falta tocarlas.
# Consigue la tuya gratis en https://console.groq.com (API Keys → Create API Key).
# Sin esta clave las funciones de IA devuelven 503 y el resto de la app funciona.
OPENAI_API_KEY=
# Opcional. Solo si quieres un modelo distinto al de Groq por defecto
# (openai/gpt-oss-120b) para resumir notas y extraer tareas.
OPENAI_MODEL=
# Opcional. Solo si quieres un modelo distinto al de Groq por defecto
# (groq/compound) para el chat del asistente. Con Groq, el modelo con buscador
# integrado no es el que conviene para resumir/extraer tareas — por eso van
# separados: OPENAI_MODEL para eso, este solo para el chat.
OPENAI_MODEL_ASISTENTE=
# Opcional. Solo si quieres un modelo de transcripción distinto al de Groq por
# defecto (whisper-large-v3-turbo) para "Grabar clase" (Estudio). Con OpenAI real,
# por defecto usa whisper-1.
OPENAI_MODEL_TRANSCRIPCION=
# Opcional. Solo si quieres un modelo de visión distinto al de Groq por defecto
# (meta-llama/llama-4-scout-17b-16e-instruct) para "Resolver ejercicio" (foto →
# solución, en Estudio). Con OpenAI real, por defecto usa gpt-4o-mini.
OPENAI_MODEL_VISION=
# Opcional. Para usar la API real de OpenAI, un servidor en local o cualquier otro
# proveedor compatible en vez de Groq (casi todos exponen /v1/chat/completions con
# el mismo formato):
#   OpenAI:     https://api.openai.com/v1
#   Ollama:     http://localhost:11434/v1
#   LM Studio:  http://localhost:1234/v1
# No incluyas el "/chat/completions" final, se añade solo. Con OpenAI real, el chat
# usa su propio buscador integrado (Responses API) en vez del de Groq — no hace
# falta tocar OPENAI_MODEL_ASISTENTE, se pone solo a gpt-4o-mini.
OPENAI_BASE_URL=

# --- Stripe -----------------------------------------------------------------
# Sin estas variables la app funciona entera, solo que nadie puede pasar de Free:
# los botones de contratación se sustituyen por un aviso.
# Empieza con las claves de test (sk_test_…) y no pases a live hasta haber probado
# el flujo completo con `stripe listen`.
STRIPE_SECRET_KEY=

# Signing secret del endpoint del webhook (whsec_…). Sin él, el webhook rechaza
# todo: es lo único que impide que cualquiera se regale el plan Team con un curl.
STRIPE_WEBHOOK_SECRET=

# Price IDs (price_…, NO los prod_…) de cada plan de pago.
# Estos son los de LIVE, ya creados en la cuenta. Solo funcionan con una sk_live_…:
# con una clave de test, Stripe responde «No such price». Para desarrollar, crea los
# mismos productos en modo test y pon aquí sus IDs.
STRIPE_PRICE_PRO=price_1U17pMA0ZutwmmcPlc4qgLvE
STRIPE_PRICE_TEAM=price_1U17pWA0ZutwmmcPAth6dFrK

# --- General ----------------------------------------------------------------
# URL canónica para metadatos y para las URLs de vuelta de Stripe Checkout.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Correos con plan Team gratis sin pasar por Stripe, separados por comas
# (ej. tucorreo@gmail.com,otro@dominio.com). Vacío por defecto: nadie tiene
# acceso especial hasta que se configure. Va aquí y no en el código para no
# dejar un correo real escrito en el historial de git.
ADMIN_EMAILS=

# --- Notificaciones push (recordatorios) -------------------------------------
# Par de claves propio de Notiq, no una cuenta externa: se genera una vez con
#   npx web-push generate-vapid-keys
# y no caduca. Sin estas dos, "Activar notificaciones" en Ajustes no aparece y
# el cron de recordatorios no manda nada (pero el resto de la app funciona
# igual). La pública lleva NEXT_PUBLIC_ porque el navegador la necesita para
# suscribirse — no es sensible, es la que identifica a Notiq como remitente.
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
# Opcional. mailto: o URL de contacto que exige el estándar Web Push para
# identificar al remitente ante el navegador. Por defecto usa el correo de
# contacto de las páginas legales.
VAPID_SUBJECT=mailto:pabloangelmendoza82@gmail.com

# Protege app/api/cron/recordatorios: Vercel manda este mismo valor en el
# header Authorization cuando dispara el cron (ver vercel.json), a partir de
# esta misma variable — sin ella, cualquiera podría llamar a esa ruta desde
# fuera. Genera un valor aleatorio, por ejemplo con:
#   openssl rand -hex 24
CRON_SECRET=
