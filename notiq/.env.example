# Notiq — variables de entorno.
# Copia este fichero a .env.local y rellena lo que necesites.

# --- Supabase ---------------------------------------------------------------
# Sin estas dos la app arranca, pero enseña un aviso de configuración en lugar de
# la aplicación.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Solo servidor. Nunca con prefijo NEXT_PUBLIC_: se salta RLS.
# Hará falta para el webhook de Stripe y para los recordatorios programados.
SUPABASE_SERVICE_ROLE_KEY=

# --- IA ---------------------------------------------------------------------
# Sin esta clave las funciones de IA devuelven 503 y el resto de la app funciona.
OPENAI_API_KEY=
# Opcional. Por defecto gpt-4o-mini.
OPENAI_MODEL=

# --- General ----------------------------------------------------------------
# URL canónica para metadatos y para el redirect del magic link.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
